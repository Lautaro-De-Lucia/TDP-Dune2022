#pragma once

#pragma once
/*
#include <fstream>
#include <iostream>
#include <string>
#include <vector>
#include <sstream>
#include "common_utils.h"


class MapHandler {
 private:
    std::ifstream text_file;

    std::string remove_spaces(std::string old_string);
    std::vector<std::string> split_line(std::string token, char delimiter);

 public:
    explicit MapHandler(std::string filename);
    ~MapHandler();
    std::vector<std::vector<cell_t>> generateMap();
};
*/